#lang:tamil

# முதல் மற்றும் இரண்டாம் எங்களை விவரிக்கிறோம்

முழு எண் முதல் = 0;
முழு எண் இரண்டு = 1;
முழு எண் மூன்று = 1;

# அச்சு (" எத்தனை எங்கள் வரை பிபோனச்சி எங்களை அச்சு செய்வது ?: ");

முழு எண் முறை = 10;

அச்சு ("%d முறை பிபோனச்சி எண்கள்: \n", முறை+2);
அச்சு ("%d ", முதல்);
அச்சு ("%d ", இரண்டு);
ஆக (முழு எண் ஐ = 0; ஐ < முறை; ஐ++){
    மூன்று = முதல் + இரண்டு;
    அச்சு ("%d ", மூன்று);
    முதல் = இரண்டு;
    இரண்டு = மூன்று;
}
அச்சு ("\n");